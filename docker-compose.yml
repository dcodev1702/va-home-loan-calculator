services:
  sentinel-va:
    build: .
    user: "1000:1000"
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    ports:
      - "3000:3000"
    volumes:
      # Persist saved scenarios on the host between runs.
      - ./data:/app/data
    restart: unless-stopped
